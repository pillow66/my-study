<template>
  <h3>Toward Modular, Reusable Code</h3>
</template>

<script>
  import * as R from 'ramda'

  /**
   * 方法链 vs 函数管道
   * 在FP中函数是输入和输出的映射(mapping)
   */
    //isEmpty :: String -> Boolean
  const isEmpty = input => !input

  /**
   * 方法链
   * 上一章，使用lodash的chain包裹使得可链式调用方法，但这并不够自然与灵活，不能链式调用其他库或自己的方法
   *
   */

  export default {}
</script>

<style lang="scss" scoped>
</style>
